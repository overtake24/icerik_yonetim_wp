<h1>{{ title }}</h1>

{{ content|safe }}

<!-- SEO -->
<meta name="description" content="{{ meta_desc }}">
<div class="tags">
{% for tag in tags %}
    <span class="tag">{{ tag }}</span>
{% endfor %}
</div>